package com.mindcraftmod.block;

import net.minecraft.block.PaneBlock;

/**
 * Rusted Iron Bars — connects like vanilla iron bars (PaneBlock),
 * but uses the rusted texture. No special behaviour beyond connection logic.
 */
public class RustedIronBarsBlock extends PaneBlock {

    public RustedIronBarsBlock(Settings settings) {
        super(settings);
    }
}
